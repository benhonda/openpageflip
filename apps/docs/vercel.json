{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && bunx bun@1.4.0 install --frozen-lockfile",
  "buildCommand": "test -n \"$VERCEL_PROJECT_PRODUCTION_URL\" || { echo 'VERCEL_PROJECT_PRODUCTION_URL is unset: astro.config.ts needs it for canonical URLs, the sitemap and og:image. Turn on \"Automatically expose System Environment Variables\" in the Vercel project settings.'; exit 1; }; bunx astro build"
}
