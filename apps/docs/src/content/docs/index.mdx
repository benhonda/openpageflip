---
# The <title> reads "<this> | OpenPageFlip", so the page title says what the site is rather than
# repeating its name; the hero keeps the name. No description here: the site-wide one applies.
title: Realistic page flips for HTML content
template: splash
hero:
  title: OpenPageFlip
  # The tagline can't import site.ts (frontmatter), so the upstream link is written out here too.
  tagline: The page-turn effect from <a href="https://github.com/Nodlik/StPageFlip" target="_blank" rel="noopener">StPageFlip</a>, rebuilt for the browsers, TypeScript and React we have now. The book below is the real thing - grab a corner and pull.
  actions:
    - text: Get started
      link: /start/core/
      icon: right-arrow
---

import CoreExample from "../../components/CoreExample.astro";

<CoreExample name="quickstart" showCode={false} />
