---
title: Playground
description: Every option as a control, the book's API as buttons, and the code that gives you exactly what you're looking at.
sidebar:
  order: 0
---

import PlaygroundExample from "../../../components/PlaygroundExample.astro";

Change anything and the book is rebuilt with the new options, on the page it was showing. The code at the bottom is generated from the controls, so it always matches the book above it - copy it into your app and you get exactly this book.

<PlaygroundExample />
