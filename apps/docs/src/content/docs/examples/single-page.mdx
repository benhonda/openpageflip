---
title: One page at a time
description: A fixed-size book that shows a single page and only turns from its corners.
---

import CoreExample from "../../../components/CoreExample.astro";

`layout: "single"` keeps one page on screen whatever the container's width, `click: "corners"` leaves the middle of the page to whatever it holds, and the fixed `width` and `height` are used as they are.

<CoreExample name="single-page" />
