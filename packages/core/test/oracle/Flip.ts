// @ts-nocheck -- vendored original, checked only by the parity tests
// Extracted verbatim from Nodlik/StPageFlip@ab30ecc src/Flip/Flip.ts (MIT): only the two enums FlipCalculation needs.

/**
 * Flipping direction
 */
export const enum FlipDirection {
    FORWARD,
    BACK,
}

/**
 * Active corner when flipping
 */
export const enum FlipCorner {
    TOP = 'top',
    BOTTOM = 'bottom',
}
