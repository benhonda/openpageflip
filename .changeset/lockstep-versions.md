---
"@openpageflip/core": patch
"@openpageflip/react": patch
---

`@openpageflip/core` and `@openpageflip/react` now share one version number and always release together, so matching versions are the ones built and tested against each other. No code changed in this release; react jumps from 0.2.0 to meet core.
