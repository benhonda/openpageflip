---
"@openpageflip/core": patch
---

A hovered corner lifts and drops over at least a quarter of `flipDuration` instead of snapping, and the same floor applies to every short animation path, such as a corner released close to where it started. While a corner is hovered the pointer takes over from the lift animation instead of fighting it.
