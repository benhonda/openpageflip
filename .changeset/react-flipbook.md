---
"@openpageflip/react": minor
---

`FlipBook` and `Page`: React 19 bindings with `ref` as a prop (the core `Book`), a controlled `page` prop, event props typed from the core, reactive options, children that can change without a remount, StrictMode-safe mount and unmount, and server rendering.
